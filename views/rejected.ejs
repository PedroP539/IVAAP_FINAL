<!DOCTYPE html>
<html lang="pt-PT">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IVAAP - Imagens Rejeitadas</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap" rel="stylesheet">
    <!-- LIGHTBOX CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/css/lightbox.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
            font-family: 'Quicksand', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .sticky-nav {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        .content {
            padding: 2rem 0;
            flex: 1;
        }

        .page-title {
            text-align: center;
            color: #27ae60;
            font-weight: bold;
            font-size: 2.2rem;
            margin-bottom: 1.5rem;
        }

        .total-count {
            text-align: center;
            color: #27ae60;
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 2rem;
            padding: 1rem;
            background: rgba(39, 174, 96, 0.1);
            border-radius: 15px;
        }

        .carousel-inner img {
            width: 100%;
            height: 280px;
            object-fit: cover;
            border-radius: 20px 20px 0 0;
        }

        .image-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            overflow: hidden;
            transition: transform 0.2s;
            margin-bottom: 1.5rem;
        }

        .image-card:hover {
            transform: translateY(-5px);
        }

        .card-img-container {
            position: relative;
        }

        .rejected-badge {
            position: absolute;
            top: 12px;
            right: 12px;
            background: #e74c3c;
            color: white;
            padding: 0.35rem 0.9rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: bold;
            z-index: 100;
        }

        .carousel-indicators {
            bottom: -40px;
        }

        .carousel-indicators li {
            background-color: #d1d1d1;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            margin: 0 4px;
        }

        .carousel-indicators .active {
            background-color: #27ae60;
            width: 14px;
            height: 14px;
        }

        .carousel {
            overflow: visible;
            margin-bottom: 40px;
            /* Space for indicators */
        }

        /* High contrast arrows */
        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            background-color: rgba(0, 0, 0, 0.6);
            /* Dark background */
            border-radius: 50%;
            background-size: 50%;
            /* Smaller arrow icon inside circle */
            width: 40px;
            height: 40px;
        }

        .carousel-control-prev,
        .carousel-control-next {
            width: 10%;
            opacity: 0.8;
        }

        .carousel-control-prev:hover,
        .carousel-control-next:hover {
            opacity: 1;
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-title {
            font-size: 1.3rem;
            font-weight: bold;
            color: #27ae60;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .btn-group-inline {
            display: flex;
            justify-content: center;
            gap: 0.75rem;
            margin-top: 1rem;
        }

        .action-btn {
            flex: 1;
            min-width: 90px;
            border-radius: 50px;
            padding: 0.7rem 0.5rem;
            font-weight: bold;
            font-size: 0.9rem;
            text-align: center;
            white-space: nowrap;
            transition: all 0.2s;
        }

        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.25);
        }

        .btn-view {
            background: #16a085;
            color: white;
        }

        .no-images {
            text-align: center;
            color: #666;
            font-style: italic;
            font-size: 1.1rem;
            padding: 3rem;
        }

        .especie {
            font-size: 1.65rem;
            background: #27AE60;
            background: repeating-linear-gradient(to top right, #27AE60 0%, #1C8247 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .variedade {
            font-size: 1.25rem;
            color: #1c8247;
            font-weight: 300;
        }

        .time-info {
            font-size: 0.85rem;
            color: #666;
            margin-top: 0.3rem;
        }

        .upload-info {
            font-size: 0.85rem;
            color: #666;
            margin-top: 0.3rem;
        }

        @media (max-width: 768px) {
            .carousel-inner img {
                height: 200px;
            }

            .btn-group-inline {
                flex-direction: column;
            }

            .action-btn {
                max-width: none;
            }
        }

        input#searchQuery,
        input#searchDate,
        input#searchUser,
        button.btn.btn-success.btn-lg.rounded-pill.w-100.h-100.d-flex.align-items-center.justify-content-center {
            font-size: 1rem;
        }
    </style>
</head>

<body>
    <header class="fixed-top">
        <div class="bg-success text-white text-center py-3">
            <% if (user) { %>
                <strong>Olá <%= user.nome %>
                        <%= user.apelido || '' %></strong>
                — último login: <%= new Date().toLocaleString('pt-PT', {day:'2-digit', month:'2-digit', year:'numeric',
                    hour:'2-digit', minute:'2-digit'}) %>
                    <% } else { %>
                        <strong>Bem-vindo ao IVAAP</strong>
                        <% } %>
        </div>
        <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
            <a class="navbar-brand font-weight-bold" href="/">IVAAP</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/upload"><i class="fa-solid fa-upload"></i>
                            CARREGAR</a></li>
                    <li class="nav-item"><a class="nav-link" href="/review"><i class="fa-solid fa-pencil"></i> REVER</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="/approved"><i class="fa-solid fa-thumbs-up"></i>
                            APROVADAS</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/rejected"><i
                                class="fa-solid fa-thumbs-down"></i> REJEITADAS</a></li>
                    <li class="nav-item"><a class="nav-link" href="/statistics"><i class="fa-solid fa-bolt"></i>
                            ESTATÍSTICAS</a></li>
                    <li class="nav-item"><a class="nav-link" href="/logout"><i
                                class="fa-solid fa-right-from-bracket"></i> SAIR</a></li>
                </ul>
            </div>
        </nav>
    </header>
    <br><br><br><br>
    <!-- BARRA DE BUSCA -->
    <div class="container mt-4 mb-3">
        <div class="card border-0 shadow-sm" style="border-radius: 20px;">
            <div class="card-body p-4">
                <form id="searchForm" action="/search" method="GET" class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label for="searchQuery" class="form-label text-success font-weight-bold">Nome da Planta</label>
                        <input type="text" class="form-control form-control-lg rounded-pill" id="searchQuery" name="q"
                            placeholder="Ex: Tomate, Rosa...">
                    </div>
                    <div class="col-md-3">
                        <label for="searchDate" class="form-label text-success font-weight-bold">Data</label>
                        <input type="date" class="form-control form-control-lg rounded-pill" id="searchDate"
                            name="date">
                    </div>
                    <div class="col-md-3">
                        <label for="searchUser" class="form-label text-success font-weight-bold">Utilizador</label>
                        <input type="text" class="form-control form-control-lg rounded-pill" id="searchUser" name="user"
                            placeholder="Ex: admin">
                    </div>
                    <div class="col-md-2">
                        <button type="submit"
                            class="btn btn-success btn-lg rounded-pill w-100 h-100 d-flex align-items-center justify-content-center">Pesquisar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <main class="container content">
        <h2 class="text-center mb-4" style="color: #27ae60; font-weight: bold;">IMAGENS REJEITADAS</h2>
        <% if (error) { %>
            <div class="alert alert-danger text-center"><strong>ERRO:</strong>
                <%= error %>
            </div>
            <% } %>
                <% if (success) { %>
                    <div class="alert alert-success text-center"><strong>SUCESSO:</strong>
                        <%= success %>
                    </div>
                    <% } %>
                        <% if (images && images.length> 0) { %>
                            <div class="total-count">
                                <i class="fas fa-times-circle"></i> Total de Imagens Rejeitadas: <strong>
                                    <%= images.length %>
                                </strong>
                            </div>
                            <% } %>
                                <div class="row" id="images-container">
                                    <% if (groupedImages && groupedImages.length> 0) { %>
                                        <% groupedImages.forEach(group=> { %>
                                            <div class="col-md-6 col-lg-4 mb-4">
                                                <div class="image-card">
                                                    <div class="card-img-container">
                                                        <div id="carousel-<%= group.key %>" class="carousel slide"
                                                            data-ride="carousel" data-interval="false">
                                                            <% if (group.images.length> 1) { %>
                                                                <ol class="carousel-indicators">
                                                                    <% group.images.forEach((img, i)=> { %>
                                                                        <li data-target="#carousel-<%= group.key %>"
                                                                            data-slide-to="<%= i %>"
                                                                            class="<%= i === 0 ? 'active' : '' %>"></li>
                                                                        <% }); %>
                                                                </ol>
                                                                <% } %>
                                                                    <div class="carousel-inner">
                                                                        <% group.images.forEach((img, i)=> { %>
                                                                            <div
                                                                                class="carousel-item <%= i === 0 ? 'active' : '' %>">
                                                                                <a href="/uploads/<%= img.image_url %>"
                                                                                    data-lightbox="rejected-group-<%= group.key %>"
                                                                                    data-title="<%= group.species %>">
                                                                                    <img src="/uploads/<%= img.image_url %>"
                                                                                        alt="<%= group.species %>"
                                                                                        onerror="this.src='/images/placeholder.jpg'">
                                                                                </a>
                                                                            </div>
                                                                            <% }); %>
                                                                    </div>
                                                                    <% if (group.images.length> 1) { %>
                                                                        <a class="carousel-control-prev"
                                                                            href="#carousel-<%= group.key %>"
                                                                            role="button" data-slide="prev">
                                                                            <span class="carousel-control-prev-icon"
                                                                                aria-hidden="true"></span>
                                                                            <span class="sr-only">Anterior</span>
                                                                        </a>
                                                                        <a class="carousel-control-next"
                                                                            href="#carousel-<%= group.key %>"
                                                                            role="button" data-slide="next">
                                                                            <span class="carousel-control-next-icon"
                                                                                aria-hidden="true"></span>
                                                                            <span class="sr-only">Seguinte</span>
                                                                        </a>
                                                                        <% } %>
                                                        </div>
                                                        <span class="rejected-badge">REJEITADA</span>
                                                    </div>
                                                    <div class="card-body">
                                                        <h5 class="card-title">
                                                            <span class="especie">
                                                                <%= group.species || 'Sem espécie' %>
                                                            </span>
                                                            <br>
                                                            <% if (group.variety) { %>
                                                                <small class="variedade">
                                                                    <%= group.variety %>
                                                                </small>
                                                                <% } %>
                                                        </h5>
                                                        <p class="text-center text-muted small mb-2">
                                                            <b>Rejeitada por:</b>
                                                            <%= group.reviewed_by || 'Sistema' %><br>
                                                                <span class="time-info">
                                                                    <i class="far fa-clock"></i>
                                                                    <%= new
                                                                        Date(group.reviewed_at).toLocaleDateString('pt-PT',
                                                                        { day: '2-digit' , month: '2-digit' ,
                                                                        year: 'numeric' , hour: '2-digit' ,
                                                                        minute: '2-digit' }) %>
                                                                </span><br>
                                                                <span class="upload-info"><br>
                                                                    <i class="fas fa-upload"></i>
                                                                    <b>Carregada por:<br></b>
                                                                    <%= group.uploaded_by || 'Desconhecido' %>
                                                                </span>
                                                                <br><strong>
                                                                    <%= group.images.length %> imagem(ns) rejeitada(s)
                                                                </strong>
                                                        </p>
                                                        <div class="btn-group-inline">
                                                            <a href="/details/<%= group.sample_id %>"
                                                                class="btn btn-view action-btn">Ver</a>
                                                        </div>

                                                        <!-- Botão Remover da DB - só aparece para admin ou quem carregou -->
                                                        <% if (user && (user.cargo==='Administrador' ||
                                                            group.uploaded_by===user.username)) { %>
                                                            <div class="text-center mt-3">
                                                                <% group.images.forEach(img=> { %>
                                                                    <form action="/delete-image/<%= img.id %>"
                                                                        method="POST" class="d-inline"
                                                                        style="display:inline;">
                                                                        <button type="submit"
                                                                            class="btn btn-danger btn-sm btn-action"
                                                                            onclick="return confirm('⚠️ Tens a certeza que queres apagar esta imagem PERMANENTEMENTE da base de dados?\nEsta ação não pode ser desfeita.');">
                                                                            <i class="fas fa-trash-alt"></i> REMOVER
                                                                            IMAGEM DA DB
                                                                        </button>
                                                                    </form>
                                                                    <% }); %>
                                                            </div>
                                                            <% } %>
                                                    </div>
                                                </div>
                                            </div>
                                            <% }); %>
                                                <% } else { %>
                                                    <div class="col-12">
                                                        <div class="no-images">
                                                            <i class="fas fa-times-circle fa-3x text-danger mb-3"></i>
                                                            <p>Nenhuma imagem rejeitada.</p>
                                                        </div>
                                                    </div>
                                                    <% } %>
                                </div>
    </main>
    <footer class="mt-5 py-4 text-center bg-light">
        <p class="mb-0">FLORA LUSITANA - © 2025 IVAAP_2 - V. 6.9.8</p>
    </footer>
    <!-- SCRIPTS -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/js/lightbox.min.js"></script>
    <script>
        lightbox.option({
            resizeDuration: 200,
            wrapAround: true,
            disableScrolling: true,
            albumLabel: "Imagem %1 de %2"
        });
        setTimeout(() => $('.alert').fadeOut(1000), 5000);
        $('.carousel').carousel();
    </script>
</body>

</html>