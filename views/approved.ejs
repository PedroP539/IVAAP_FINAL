<!DOCTYPE html>
<html lang="pt-PT">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IVAAP - Imagens Aprovadas</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/css/lightbox.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
            font-family: 'Quicksand', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .sticky-nav {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        .content {
            padding: 2rem 0;
            flex: 1;
        }

        .page-title {
            text-align: center;
            color: #27ae60;
            font-weight: bold;
            font-size: 2.2rem;
            margin-bottom: 1.5rem;
        }

        .total-count {
            text-align: center;
            color: #27ae60;
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 2rem;
            padding: 1rem;
            background: rgba(39, 174, 96, 0.1);
            border-radius: 15px;
        }

        .image-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            overflow: hidden;
            transition: transform 0.2s;
            margin-bottom: 1.5rem;
        }

        .image-card:hover {
            transform: translateY(-5px);
        }

        .card-img-container {
            position: relative;
            height: 280px;
            overflow: hidden;
            border-radius: 20px 20px 0 0;
        }

        .card-img-top {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .image-card:hover .card-img-top {
            transform: scale(1.05);
        }

        .approved-badge {
            position: absolute;
            top: 12px;
            right: 12px;
            background: #27ae60;
            color: white;
            padding: 0.35rem 0.9rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: bold;
            z-index: 100;
        }

        .new-badge {
            position: absolute;
            top: 12px;
            left: 12px;
            background: #e74c3c;
            color: white;
            padding: 0.35rem 0.9rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: bold;
            z-index: 100;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {

            0%,
            100% {
                opacity: 1;
            }

            50% {
                opacity: 0.7;
            }
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-title {
            font-size: 1.3rem;
            font-weight: bold;
            color: #27ae60;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .btn-group-inline {
            display: flex;
            justify-content: center;
            gap: 0.75rem;
            margin-top: 1rem;
        }

        .action-btn {
            flex: 1;
            min-width: 90px;
            border-radius: 50px;
            padding: 0.7rem 0.5rem;
            font-weight: bold;
            font-size: 0.9rem;
            text-align: center;
            white-space: nowrap;
            transition: all 0.2s;
        }

        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.25);
        }

        .btn-view {
            background: #16a085;
            color: white;
        }

        .no-images {
            text-align: center;
            color: #666;
            font-style: italic;
            font-size: 1.1rem;
            padding: 3rem;
        }

        .especie {
            font-size: 1.65rem;
            background: #27AE60;
            background: repeating-linear-gradient(to top right, #27AE60 0%, #1C8247 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .variedade {
            font-size: 1.25rem;
            color: #1c8247;
            font-weight: 300;
        }

        .time-info {
            font-size: 0.85rem;
            color: #666;
            margin-top: 0.5rem;
        }

        .upload-info {
            font-size: 0.85rem;
            color: #666;
            margin-top: 0.3rem;
        }

        .scroll-to-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: #27ae60;
            color: white;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: none;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s;
            z-index: 999;
        }

        .scroll-to-top:hover {
            background: #1e8449;
            transform: translateY(-5px);
        }

        @media (max-width: 768px) {
            .btn-group-inline {
                flex-direction: column;
            }

            .action-btn {
                max-width: none;
            }

            .card-img-container {
                height: 200px;
            }
        }

        input#searchQuery,
        input#searchDate,
        input#searchUser,
        button.btn.btn-success.btn-lg.rounded-pill.w-100.h-100.d-flex.align-items-center.justify-content-center {
            font-size: 1rem;
        }

        .btn-group-sm>.btn,
        .btn-sm {
            padding: .8rem 1rem;
            font-size: .875rem;
            line-height: 1.5;
            border-radius: 50px;
        }
    </style>
</head>

<body>
    <header class="fixed-top">
        <div class="bg-success text-white text-center py-3">
            <% if (user) { %>
                <strong>Olá <%= user.nome %>
                        <%= user.apelido || '' %></strong>
                — último login: <%= new Date().toLocaleString('pt-PT', {day:'2-digit', month:'2-digit', year:'numeric',
                    hour:'2-digit', minute:'2-digit'}) %>
                    <% } else { %>
                        <strong>Bem-vindo ao IVAAP</strong>
                        <% } %>
        </div>
        <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
            <a class="navbar-brand font-weight-bold" href="/">IVAAP</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="/upload"><i class="fa-solid fa-upload"></i>
                            CARREGAR</a></li>
                    <li class="nav-item"><a class="nav-link" href="/review"><i class="fa-solid fa-pencil"></i> REVER</a>
                    </li>
                    <li class="nav-item"><a class="nav-link active" href="/approved"><i
                                class="fa-solid fa-thumbs-up"></i> APROVADAS</a></li>
                    <li class="nav-item"><a class="nav-link" href="/rejected"><i class="fa-solid fa-thumbs-down"></i>
                            REJEITADAS</a></li>
                    <li class="nav-item"><a class="nav-link" href="/statistics"><i class="fa-solid fa-bolt"></i>
                            ESTATÍSTICAS</a></li>
                    <li class="nav-item"><a class="nav-link" href="/logout"><i
                                class="fa-solid fa-right-from-bracket"></i> SAIR</a></li>
                </ul>
            </div>
        </nav>
    </header>
    <br><br><br><br>
    <div class="container mt-4 mb-3">
        <div class="card border-0 shadow-sm" style="border-radius: 20px;">
            <div class="card-body p-4">
                <form id="searchForm" action="/search" method="GET" class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label for="searchQuery" class="form-label text-success font-weight-bold">Nome da Planta</label>
                        <input type="text" class="form-control form-control-lg rounded-pill" id="searchQuery" name="q"
                            placeholder="Ex: Tomate, Rosa...">
                    </div>
                    <div class="col-md-3">
                        <label for="searchDate" class="form-label text-success font-weight-bold">Data</label>
                        <input type="date" class="form-control form-control-lg rounded-pill" id="searchDate"
                            name="date">
                    </div>
                    <div class="col-md-3">
                        <label for="searchUser" class="form-label text-success font-weight-bold">Utilizador</label>
                        <input type="text" class="form-control form-control-lg rounded-pill" id="searchUser" name="user"
                            placeholder="Ex: admin">
                    </div>
                    <div class="col-md-2">
                        <button type="submit"
                            class="btn btn-success btn-lg rounded-pill w-100 h-100 d-flex align-items-center justify-content-center">Pesquisar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <main class="container content">
        <h2 class="text-center mb-4" style="color: #27ae60; font-weight: bold;">IMAGENS APROVADAS</h2>

        <% if (images && images.length> 0) { %>
            <div class="total-count">
                <i class="fas fa-check-circle"></i> Total de Plantas Aprovadas: <strong>
                    <%= images.length %>
                </strong>
            </div>
            <% } %>

                <% if (error) { %>
                    <div class="alert alert-danger text-center alert-dismissible fade show">
                        <strong>ERRO:</strong>
                        <%= error %>
                            <button type="button" class="close" data-dismiss="alert">×</button>
                    </div>
                    <% } %>
                        <% if (success) { %>
                            <div class="alert alert-success text-center alert-dismissible fade show">
                                <strong>SUCESSO:</strong>
                                <%= success %>
                                    <button type="button" class="close" data-dismiss="alert">×</button>
                            </div>
                            <% } %>
                                <div class="row" id="images-container">
                                    <% if (images && images.length> 0) { %>
                                        <% // Ordenar por data de aprovação (mais recente primeiro) const
                                            sortedImages=images.sort((a, b)=> {
                                            const dateA = new Date(a.reviewed_at || a.uploaded_at);
                                            const dateB = new Date(b.reviewed_at || b.uploaded_at);
                                            return dateB - dateA;
                                            });

                                            const now = new Date();
                                            const oneDayAgo = new Date(now - 24 * 60 * 60 * 1000);
                                            %>

                                            <% sortedImages.forEach(image=> {
                                                const reviewedDate = new Date(image.reviewed_at || image.uploaded_at);
                                                const isNew = reviewedDate > oneDayAgo;
                                                %>
                                                <div class="col-md-6 col-lg-4 mb-4">
                                                    <div class="image-card">
                                                        <div class="card-img-container">
                                                            <!-- Imagem fixa: a vencedora (mais votada) da planta -->
                                                            <a href="/uploads/<%= image.winning_image_url %>"
                                                                data-lightbox="approved-images"
                                                                data-title="<%= image.species || 'Sem espécie' %>">
                                                                <img src="/uploads/<%= image.winning_image_url %>"
                                                                    class="card-img-top" alt="<%= image.species %>"
                                                                    onerror="this.src='/images/placeholder.jpg'">
                                                            </a>
                                                            <% if (isNew) { %>
                                                                <span class="new-badge">NOVO!</span>
                                                                <% } %>
                                                                    <span class="approved-badge">APROVADA</span>
                                                        </div>
                                                        <div class="card-body">
                                                            <h5 class="card-title">
                                                                <span class="especie">
                                                                    <%= image.species || 'Sem espécie' %>
                                                                </span>
                                                                <br>
                                                                <% if (image.variety) { %>
                                                                    <small class="variedade">
                                                                        <%= image.variety %>
                                                                    </small>
                                                                    <% } %>
                                                            </h5>
                                                            <p class="text-center text-muted small mb-2">
                                                                <strong>Gama:</strong>
                                                                <%= image.gama_nome || 'Não definido' %><br><br>
                                                                    <b>Aprovada por:</b>
                                                                    <%= image.reviewed_by || 'Sistema' %><br>
                                                                        <span class="time-info">
                                                                            <i class="far fa-clock"></i>
                                                                            <%= reviewedDate.toLocaleDateString('pt-PT',
                                                                                { day: '2-digit' , month: '2-digit' ,
                                                                                year: 'numeric' , hour: '2-digit' ,
                                                                                minute: '2-digit' }) %>
                                                                        </span><br>
                                                                        <span class="upload-info">
                                                                            <br>
                                                                            <i class="fas fa-upload"></i>
                                                                            Carregada por:<br>
                                                                            <%= image.uploaded_by || 'Desconhecido' %> •
                                                                                <%= new
                                                                                    Date(image.uploaded_at).toLocaleDateString('pt-PT')
                                                                                    %>
                                                                        </span>
                                                            </p>
                                                            <div class="btn-group-inline">
                                                                <button class="btn btn-success action-btn"
                                                                    disabled>APROVADA</button>
                                                                <a href="/details/<%= image.id %>"
                                                                    class="btn btn-view action-btn">VER</a>
                                                            </div>

                                                            <!-- Botão Remover da DB - só aparece para admin ou quem carregou -->
                                                            <% if (user && (user.cargo==='Administrador' ||
                                                                image.uploaded_by===user.username)) { %>
                                                                <div class="text-center mt-3">
                                                                    <form action="/delete-image/<%= image.id %>"
                                                                        method="POST" class="d-inline"
                                                                        style="display:inline;">
                                                                        <button type="submit"
                                                                            class="btn btn-danger btn-sm btn-action"
                                                                            onclick="return confirm('⚠️ Tens a certeza que queres apagar esta imagem PERMANENTEMENTE da base de dados?\nEsta ação não pode ser desfeita.');">
                                                                            <i class="fas fa-trash-alt"></i> REMOVER DA
                                                                            DB
                                                                        </button>
                                                                    </form>
                                                                </div>
                                                                <% } %>


                                                        </div>
                                                    </div>
                                                </div>
                                                <% }); %>
                                                    <% } else { %>
                                                        <div class="col-12">
                                                            <div class="no-images">
                                                                <i class="fas fa-inbox fa-3x mb-3 text-muted"></i>
                                                                <p class="text-muted">Nenhuma imagem aprovada ainda.</p>
                                                                <a href="/review"
                                                                    class="btn btn-warning rounded-pill px-4 mt-3">
                                                                    <i class="fas fa-search"></i> REVER PENDENTES
                                                                </a>
                                                            </div>
                                                        </div>
                                                        <% } %>
                                </div>
    </main>
    <!-- Botão Scroll to Top -->
    <div class="scroll-to-top" id="scrollToTop">
        <i class="fas fa-arrow-up fa-lg"></i>
    </div>
    <footer class="mt-5 py-4 text-center bg-light">
        <p class="mb-0">FLORA LUSITANA - © 2025 IVAAP_2 - V. 6.9.8</p>
    </footer>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.4/js/lightbox.min.js"></script>
    <script>
        lightbox.option({
            resizeDuration: 200,
            wrapAround: true,
            disableScrolling: true,
            albumLabel: "Imagem %1 de %2"
        });

        setTimeout(() => $('.alert').fadeOut(1000), 5000);

        $(window).scroll(function () {
            if ($(this).scrollTop() > 300) {
                $('#scrollToTop').fadeIn();
            } else {
                $('#scrollToTop').fadeOut();
            }
        });

        $('#scrollToTop').click(function () {
            $('html, body').animate({ scrollTop: 0 }, 600);
            return false;
        });

        console.log('Total de imagens aprovadas carregadas: <%= images ? images.length : 0 %>');
    </script>
</body>

</html>