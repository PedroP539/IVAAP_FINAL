<!DOCTYPE html>
<html lang="pt-PT">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IVAAP - Entrar</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/theme-toggle.js"></script>
    <style>
        body {
            background: #d0e8d3;
            font-family: 'Manrope', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .login-box {
            background: white;
            border-radius: 28px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
            padding: 3rem 2.5rem;
            max-width: 420px;
            width: 100%;
            text-align: center;
        }

        .logo {
            width: 100%;
            max-width: 280px;
            margin: 0 auto 2rem;
        }

        .logo svg {
            width: 100%;
            height: auto;
            display: block;
        }

        h3 {
            color: #27ae60;
            font-weight: 700;
            margin-bottom: 2rem;
            font-size: 1.8rem;
        }

        .form-control {
            border-radius: 50px;
            padding: 0.9rem 1.5rem;
            border: 2px solid #ced4da;
            font-size: 1.05rem;
        }

        .form-control:focus {
            border-color: #27ae60;
            box-shadow: 0 0 0 0.25rem rgba(39, 174, 96, 0.25);
        }

        .btn-login {
            background: #27ae60;
            color: white;
            border: none;
            border-radius: 50px;
            padding: 0.9rem;
            font-weight: 700;
            width: 100%;
            font-size: 1.2rem;
            margin-top: 1.5rem;
            transition: all .3s;
        }

        .btn-login:hover {
            background: #219653;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(39, 174, 96, 0.4);
        }

        .alert {
            border-radius: 50px;
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
            padding: 0.9rem 1.3rem;
            text-align: center;
        }

        .hint {
            margin-top: 2rem;
            color: #555;
            font-size: 0.95rem;
        }

        .hint strong {
            color: #27ae60;
            font-size: 1.05rem;
        }
    </style>
</head>

<body>
    <div class="login-box">

        <!-- LOGO FLORA LUSITANA -->
        <div class="logo">
            <img src="/uploads/flora-lusitana.svg" alt="Flora Lusitana">
        </div>
        <h3>LOGIN</h3>
        <!-- ALERTA SEGURO – nunca mais dá erro -->
        <% if (typeof message==='string' && message.trim() !=='' ) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= message %>
                    <button type="button" class="close" data-dismiss="alert">
                        <span>&times;</span>
                    </button>
            </div>
            <% } %>

                <form method="POST" action="/login">
                    <div class="form-group">
                        <input type="text" name="username" class="form-control" placeholder="Utilizador" required
                            autofocus>
                    </div>
                    <div class="form-group">
                        <input type="password" name="password" class="form-control" placeholder="Password" required>
                    </div>
                    <button type="submit" class="btn-premium btn-premium-success mt-3 w-100">
                        Entrar
                    </button>
                </form>

    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
    <script>
        // Fecha o alerta automaticamente após 4 segundos
        setTimeout(() => $('.alert').alert('close'), 4000);
    </script>


    <!--	<link rel="stylesheet" href="/css/glitch.css">
	<script src="/js/glitch.js"></script>

	
	<link rel="stylesheet" href="/css/flicker.css">
	<script src="/js/lamp.js"></script>
-->

</body>

</html>