<!DOCTYPE html>
<html lang="pt-PT">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IVAAP - Recuperar Password</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background: #d0e8d3;
            font-family: 'Manrope', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .recovery-box {
            background: white;
            border-radius: 28px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
            padding: 3rem 2.5rem;
            max-width: 420px;
            width: 100%;
            text-align: center;
        }

        .logo {
            width: 100%;
            max-width: 280px;
            margin: 0 auto 2rem;
        }

        h3 {
            color: #27ae60;
            font-weight: 700;
            margin-bottom: 1rem;
            font-size: 1.6rem;
        }

        .subtitle {
            color: #666;
            margin-bottom: 2rem;
            font-size: 0.95rem;
        }

        .form-control {
            border-radius: 50px;
            padding: 0.9rem 1.5rem;
            border: 2px solid #ced4da;
            font-size: 1.05rem;
        }

        .form-control:focus {
            border-color: #27ae60;
            box-shadow: 0 0 0 0.25rem rgba(39, 174, 96, 0.25);
        }

        .alert {
            border-radius: 50px;
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
            padding: 0.9rem 1.3rem;
        }

        .back-link {
            margin-top: 2rem;
            color: #27ae60;
            font-weight: 600;
            text-decoration: none;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="recovery-box">
        <div class="logo">
            <img src="/uploads/flora-lusitana.svg" alt="Flora Lusitana">
        </div>
        <h3>RECUPERAR PASSWORD</h3>
        <p class="subtitle">Introduz o teu utilizador ou email para receber instruções de recuperação.</p>

        <% if (typeof error !=='undefined' && error) { %>
            <div class="alert alert-danger alert-dismissible fade show">
                <%= error %>
                    <button type="button" class="close" data-dismiss="alert">×</button>
            </div>
            <% } %>

                <% if (typeof success !=='undefined' && success) { %>
                    <div class="alert alert-success alert-dismissible fade show">
                        <%= success %>
                            <button type="button" class="close" data-dismiss="alert">×</button>
                    </div>
                    <% } %>

                        <form method="POST" action="/forgot-password">
                            <div class="form-group">
                                <input type="text" name="identifier" class="form-control"
                                    placeholder="Utilizador ou E-mail" required>
                            </div>
                            <button type="submit" class="btn-premium btn-premium-success mt-3 w-100">
                                Enviar Instruções
                            </button>
                        </form>

                        <p class="mt-4"><a href="/login" class="back-link"><i class="fas fa-arrow-left"></i> Voltar ao
                                Login</a></p>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
    <script>
        setTimeout(() => $('.alert').alert('close'), 5000);
    </script>
</body>

</html>